spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.14.101:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml

